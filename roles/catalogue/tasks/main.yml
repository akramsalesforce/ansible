-  name: setup nodejs
   ansible.builtin.shell: curl -fsSL https://rpm.nodesource.com/setup_lts.x | bash -


- name: Install nodeJS
  ansible.builtin.yum:
    name:
      - nodejs
      - gcc-c++
    state: installed





